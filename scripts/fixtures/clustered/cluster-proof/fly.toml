app = 'mesh-cluster-proof'
primary_region = 'iad'

[build]
  dockerfile = 'scripts/fixtures/clustered/cluster-proof/Dockerfile'

[env]
  MESH_CLUSTER_PORT = '4370'
  MESH_DISCOVERY_SEED = 'mesh-cluster-proof.internal'

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
